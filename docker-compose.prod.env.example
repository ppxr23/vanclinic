# Copier ce fichier en ".env" à la racine du projet sur le VPS (jamais commité).
# docker compose le charge automatiquement s'il se trouve à côté de docker-compose.prod.yml.

DOMAIN=vanclinic.pro
LETSENCRYPT_EMAIL=admin@vanclinic.pro
POSTGRES_PASSWORD=change_me_openssl_rand_hex_16
